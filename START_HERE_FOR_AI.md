# Start Here

You are in the correct XR ViewLab source tree if your current folder is:

`F:\ViewLab`

The goal file is:

`F:\ViewLab\PROJECT_GOAL.md`

Read that first, update it as you work, then build from `F:\ViewLab`.

Do not reference or depend on files outside `F:\ViewLab`.
