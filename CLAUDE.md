# CLAUDE.md — compatibility shim

Canonical project memory lives in the repository, not in this file:

1. `agents.md` — operating manual, rules, navigation index (read first)
2. `STATE.md` — live project state, active milestone, known issues
3. `repo-index.json` — machine index: subsystem → files/symbols/tests/docs
4. `docs/` — architecture, config contract, decisions, regressions, verification

Do not add project knowledge here; update the canonical files above instead.
